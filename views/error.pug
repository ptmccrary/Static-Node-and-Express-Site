extends layout.pug

block content
    nav.grid-container.portfolio-breadcrumb
        a(href='/') ← Back
    article.grid-container.portfolio-intro.portfolio-project
        .grid-x
            .cell.small-6.medium-9.large-9
                img(src='../static/images/page-not-found.svg' alt='Not found image' style='width:50%; height:30%; text-align:center')
                h1= error.message
                h2= `ERROR CODE: ${err.status}`
                p= err.stack
